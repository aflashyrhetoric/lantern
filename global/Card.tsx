import React, { useState } from "react";

interface AppledoreCardProps {
  name: string;
}

const AppledoreCard: React.FC<AppledoreCardProps> = (props: AppledoreCardProps) => {
  // const [something, setSomething] = useState(props.something);

  return (
    <></>
  );
};

export default AppledoreCard;
